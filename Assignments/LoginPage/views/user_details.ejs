<!DOCTYPEhtml>
<html>
    <head>
        <title>User Details</title>     
        
    </head>
    <!--Add vanishing text inside text boxes. Pattern in text box so as to enter more than 4 characters. Align to centre and add boundaries. Add colour to the submit button-->
    <body>
        <form action="/database" method="POST" autocomplete="off"> <!--autocomplete="off" so that the form doesnt display any suggestions-->
            <h3>User Information</h3>
                        Name
            <input type = "text" name = "name" pattern = "[A-Za-z]{1,}" title = "Enter firstname and lastname using alphabets only" autofocus required> <!-- [A-Za-z]{1,} - {1,} specifies 1 to any number of digits -->
            <br><br>  
                        Student ID
            <input type = "text" name = "sid" pattern = "[0-9]{9}" title = "Student ID is a 9 digit number" required>
            <br><br>
                        Department
            <input type = "text" name = "dept" pattern = "[A-Za-z]{1,}" title = "Enter department using alphabets only" required>
            <br><br>
            <input type="submit" value = "Add a User" style= "background-color : lightblue"/><br><br>
            <input type = "reset" value="Clear" style="background-color :lightblue">
        </form>
        <div id="result"></div>
    </body>
</html>
